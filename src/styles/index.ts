import { createStitches } from "@stitches/react";

export const {
  config,
  css,
  styled,
  globalCss,
  getCssText,
  keyframes,
  theme,
  createTheme
} = createStitches({
  theme: {
    colors: {
      rocketseat: '#8257e6'
    }
  },
})